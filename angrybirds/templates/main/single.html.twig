{% extends 'base.html.twig' %}

{% block title %}Single Bird{% endblock %}

{% block body %}
<h1>A Field Guide to Angry Birds</h1>

<div class="container-fluid">
    <div class="row justify-content-center">

        <div class="card mb-3 col-md-6 align-self-center">
            <div class="row no-gutters">
                <div class="col-md-4">
                <img src="{{ asset('assets/images/' ~ bird.image) }}" class="card-img">
                </div>
                <div class="col-md-8">
                <div class="card-body">
                    <h5 class="card-title">{{ bird.name }}</h5>
                    <p class="card-text">{{ bird.description }}</p>
                    <hr/>
                    <a href="{{ path('main') }}">Retour à la liste</a>

                </div>
                </div>
            </div>
        </div>

    </div>
</div>
{% endblock %}

